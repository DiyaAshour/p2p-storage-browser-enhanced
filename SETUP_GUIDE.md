# P2P Storage Browser - Complete Setup Guide

## خطوات التثبيت والتشغيل

هذا الدليل يشرح كيفية تثبيت وتشغيل تطبيق P2P Storage Browser على جهازك.

### المتطلبات الأساسية

- **Node.js**: الإصدار 18 أو أحدث
- **npm أو pnpm**: مدير الحزم
- **MetaMask**: إضافة المتصفح للمحافظ الرقمية
- **نظام التشغيل**: Windows, macOS, أو Linux

### الخطوة 1: تثبيت البرنامج

```bash
# انسخ المشروع إلى جهازك
cd /path/to/p2p-desktop

# ثبت جميع المكتبات المطلوبة
pnpm install
```

### الخطوة 2: تشغيل التطبيق في بيئة التطوير

#### للويب فقط:
```bash
pnpm run dev
```

ثم افتح المتصفح على: `http://localhost:3000`

#### لتطبيق Electron (سطح المكتب):
```bash
# في نافذة Terminal الأولى
pnpm run dev

# في نافذة Terminal ثانية (بعد ثوان قليلة)
pnpm run dev:electron
```

### الخطوة 3: ربط محفظتك

1. تأكد من تثبيت MetaMask في المتصفح
2. افتح التطبيق
3. اضغط على زر "Connect Wallet"
4. وافق على الاتصال في نافذة MetaMask
5. سيظهر عنوان محفظتك ورصيدك

### الخطوة 4: رفع الملفات

1. اذهب إلى تبويب "Upload Files"
2. اختر الملفات التي تريد رفعها
3. اختياري: فعّل خيار "Index files for network search" لجعل الملفات قابلة للبحث
4. اضغط "Upload Files"

### الخطوة 5: البحث عن الملفات

1. اذهب إلى تبويب "File Browser"
2. استخدم شريط البحث للبحث عن الملفات
3. يمكنك تحميل أو حذف الملفات

## هيكل المشروع

```
p2p-storage-browser/
├── client/                    # كود React (الواجهة الأمامية)
│   ├── src/
│   │   ├── pages/
│   │   │   └── Home.tsx      # الصفحة الرئيسية
│   │   ├── components/       # مكونات واجهة المستخدم
│   │   ├── hooks/
│   │   │   ├── useWallet.ts  # إدارة المحفظة
│   │   │   └── useStorage.ts # إدارة التخزين
│   │   ├── services/
│   │   │   ├── web3Service.ts    # خدمات Web3
│   │   │   ├── p2pService.ts     # خدمات P2P
│   │   │   └── storageService.ts # خدمات التخزين
│   │   ├── App.tsx           # المكون الرئيسي
│   │   └── index.css         # الأنماط العامة
│   ├── public/               # الملفات الثابتة
│   └── index.html            # صفحة HTML الرئيسية
├── electron/                 # كود Electron (تطبيق سطح المكتب)
│   ├── main.js              # العملية الرئيسية
│   └── preload.js           # سكريبت التحضير
├── package.json             # معلومات المشروع والمكتبات
├── tsconfig.json            # إعدادات TypeScript
├── vite.config.ts           # إعدادات Vite
└── README.md                # التوثيق الرئيسي
```

## شرح المكتبات المستخدمة

### Web3 و Blockchain
- **ethers.js**: مكتبة للتفاعل مع البلوكتشين والمحافظ
- **@web3-react**: مكتبة لربط المحافظ بتطبيقات React

### P2P Networking
- **libp2p**: بروتوكول الشبكات من نظير إلى نظير
- **@libp2p/tcp**: دعم اتصالات TCP
- **@libp2p/webrtc**: دعم اتصالات WebRTC
- **@chainsafe/libp2p-gossipsub**: بروتوكول النشر والاشتراك

### التخزين
- **level**: قاعدة بيانات محلية
- **IndexedDB**: تخزين في المتصفح

### واجهة المستخدم
- **React 19**: إطار عمل لبناء الواجهات
- **Tailwind CSS**: إطار عمل للأنماط
- **shadcn/ui**: مكتبة مكونات واجهة مستخدم

### سطح المكتب
- **Electron**: لبناء تطبيقات سطح المكتب

## الأوامر المهمة

```bash
# تشغيل في بيئة التطوير
pnpm run dev

# بناء المشروع للإنتاج
pnpm run build

# تشغيل تطبيق Electron
pnpm run dev:electron

# بناء تطبيق Electron للتوزيع
pnpm run build:electron

# فحص الأخطاء
pnpm run check

# تنسيق الكود
pnpm run format
```

## نظام الدفع

### تسعير التخزين

| السعة | السعر الشهري |
|------|-----------|
| 1 TB | $1 |
| 3 TB | $3 |
| 5 TB | $5 |
| 10 TB | $10 |

### كيفية الدفع

1. اختر خطة التخزين من تبويب "Statistics"
2. سيتم حساب التكلفة الشهرية تلقائياً
3. يتم الدفع من خلال محفظتك المتصلة

## استكشاف الأخطاء

### المشكلة: لا تظهر نافذة MetaMask

**الحل:**
- تأكد من تثبيت MetaMask
- تأكد من أن MetaMask مفتوح ومفعّل
- حاول إعادة تحميل الصفحة

### المشكلة: فشل رفع الملفات

**الحل:**
- تحقق من مساحة التخزين المتاحة
- تأكد من أن حجم الملف أقل من 4GB
- تحقق من اتصالك بالإنترنت

### المشكلة: بطء الشبكة

**الحل:**
- تأكد من أن هناك أقران متصلين
- تحقق من إعدادات جدار الحماية
- أعد تشغيل التطبيق

## الميزات المتقدمة

### البحث والفهرسة

عند رفع الملفات، يمكنك اختيار:
- **Indexed**: الملف قابل للبحث على الشبكة
- **Private**: الملف خاص بك فقط

### التشفير

يمكن تشفير الملفات قبل الرفع لزيادة الأمان.

### النسخ الاحتياطية

الملفات يتم توزيعها على عدة أقران لضمان عدم فقدانها.

## الأمان

- جميع المعاملات موقعة بمفتاحك الخاص
- لا يتم تخزين كلمات المرور
- جميع الاتصالات مشفرة

## الدعم والمساعدة

إذا واجهت مشاكل:

1. تحقق من قسم استكشاف الأخطاء أعلاه
2. اقرأ ملف README.md
3. تحقق من سجلات الأخطاء في وحدة التحكم

## الخطوات التالية

بعد التثبيت الناجح:

1. تعرّف على واجهة المستخدم
2. جرّب رفع ملف صغير أولاً
3. استكشف ميزات البحث والفهرسة
4. اختبر خطط التخزين المختلفة

## ملاحظات مهمة

⚠️ **تحذير**: هذا تطبيق تجريبي. استخدمه على مسؤوليتك الخاصة.

💾 **نصيحة**: احتفظ بنسخ احتياطية من ملفاتك المهمة.

🔐 **أمان**: لا تشارك مفاتيحك الخاصة مع أحد.

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.

---

**تم آخر تحديث**: فبراير 2026
