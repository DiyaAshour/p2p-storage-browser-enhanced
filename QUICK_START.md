# P2P Storage Browser - Quick Start Guide

## البدء السريع

### المتطلبات
- Node.js 18+
- MetaMask متصفح
- نظام تشغيل: Windows, macOS, أو Linux

### الخطوة 1: تثبيت المشروع

```bash
# انسخ المشروع إلى مجلد على جهازك
cd /path/to/your/projects
git clone <repository-url>
cd p2p-storage-browser

# ثبت المكتبات
pnpm install
```

### الخطوة 2: تشغيل التطبيق

#### خيار 1: تشغيل في المتصفح (الأسهل)
```bash
pnpm run dev
```
ثم افتح المتصفح على: `http://localhost:3000`

#### خيار 2: تشغيل تطبيق سطح المكتب (Electron)
```bash
# نافذة 1
pnpm run dev

# نافذة 2 (بعد ثوان قليلة)
pnpm run dev:electron
```

### الخطوة 3: الاستخدام الأساسي

1. **ربط المحفظة**
   - اضغط "Connect Wallet"
   - وافق على الاتصال في MetaMask
   - سيظهر عنوان محفظتك

2. **رفع ملفات**
   - اذهب إلى "Upload Files"
   - اختر الملفات
   - اضغط "Upload Files"

3. **البحث عن الملفات**
   - اذهب إلى "File Browser"
   - استخدم شريط البحث
   - حمّل أو احذف الملفات

4. **إدارة التخزين**
   - اذهب إلى "Statistics"
   - اختر خطة تخزين
   - سيتم حساب التكلفة الشهرية

## الأوامر المهمة

```bash
# تشغيل في بيئة التطوير
pnpm run dev

# بناء للإنتاج
pnpm run build

# تشغيل Electron
pnpm run dev:electron

# بناء Electron
pnpm run build:electron

# فحص الأخطاء
pnpm run check

# تنسيق الكود
pnpm run format
```

## هيكل الملفات الأساسي

```
p2p-storage-browser/
├── client/src/
│   ├── pages/Home.tsx          ← الصفحة الرئيسية
│   ├── services/
│   │   ├── web3Service.ts      ← إدارة المحفظة
│   │   ├── p2pService.ts       ← الشبكة P2P
│   │   └── storageService.ts   ← إدارة الملفات
│   └── hooks/
│       ├── useWallet.ts        ← Hook المحفظة
│       └── useStorage.ts       ← Hook التخزين
├── electron/
│   ├── main.js                 ← تطبيق Electron
│   └── preload.js              ← IPC
├── package.json                ← المكتبات
└── README.md                   ← التوثيق الكامل
```

## استكشاف الأخطاء السريع

### المشكلة: لا يعمل الاتصال بـ MetaMask
**الحل:**
- تأكد من تثبيت MetaMask
- أعد تحميل الصفحة
- تحقق من أن MetaMask مفتوح

### المشكلة: فشل الرفع
**الحل:**
- تحقق من مساحة التخزين
- تأكد من اتصالك بالإنترنت
- جرب ملف أصغر

### المشكلة: بطء الشبكة
**الحل:**
- تأكد من وجود أقران متصلين
- أعد تشغيل التطبيق
- تحقق من جدار الحماية

## الميزات الرئيسية

✅ **Web3 Integration**: ربط آمن بالمحافظ الرقمية
✅ **P2P Network**: شبكة موزعة لامركزية
✅ **File Storage**: تخزين محلي وموزع
✅ **Search**: البحث عن الملفات
✅ **Payment**: نظام دفع بالعملات الرقمية
✅ **Desktop App**: تطبيق سطح مكتب كامل

## الخطوات التالية

1. اقرأ `README.md` للتوثيق الكامل
2. اقرأ `SETUP_GUIDE.md` للتفاصيل الكاملة
3. استكشف الكود في مجلد `client/src/`
4. جرب الميزات المختلفة

## نصائح مهمة

💡 **نصيحة 1**: ابدأ برفع ملفات صغيرة لاختبار النظام
💡 **نصيحة 2**: استخدم شبكة اختبار (Testnet) في البداية
💡 **نصيحة 3**: احتفظ بنسخ احتياطية من ملفاتك المهمة

## الدعم

إذا واجهت مشاكل:
1. تحقق من قسم استكشاف الأخطاء أعلاه
2. اقرأ `README.md`
3. تحقق من سجلات الأخطاء في وحدة التحكم

---

**استمتع باستخدام P2P Storage Browser!** 🚀
